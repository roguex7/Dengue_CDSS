services:
  - type: web
    name: denguecdss
    runtime: docker          # Use Dockerfile — not buildCommand
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"